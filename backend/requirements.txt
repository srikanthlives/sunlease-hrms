fastapi==0.104.1
uvicorn[standard]==0.24.0
sqlalchemy==2.0.23
pydantic==2.5.0
pydantic-settings==2.1.0
pydantic[email]==2.5.0
python-jose[cryptography]==3.3.0
bcrypt==4.0.1
python-multipart==0.0.6
python-dateutil==2.8.2

# Cloudflare R2 storage (§14 of the blueprint) - documents currently save
# to local disk (see services/document_service.py); kept here so the
# storage backend can be swapped to R2 later without a dependency churn.
boto3==1.34.162

# Bulk employee upload / downloadable template (.xlsx)
openpyxl==3.1.5
